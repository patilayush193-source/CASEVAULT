import UploadStudio from '@/components/upload/UploadStudio';

export const metadata = {
  title: 'Upload | CaseVault',
  description: 'Upload your case competition slide deck to CaseVault',
};

export default function UploadPage() {
  return <UploadStudio />;
}
